'use client';
/* eslint-disable @next/next/no-location-assign-relative-destination */

import { useState } from 'react';
import { ArrowUpRight, CalendarDays, TrendingUp } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';

const series = {
  Copper: { hi: 'तांबा', rate: 470, values: [418, 425, 431, 428, 442, 450, 463, 470], color: 'bg-blue-600' },
  PCB: { hi: 'मदरबोर्ड', rate: 335, values: [298, 305, 301, 314, 319, 326, 330, 335], color: 'bg-emerald-600' },
  Aluminium: { hi: 'एल्युमिनियम', rate: 185, values: [165, 168, 171, 174, 176, 180, 182, 185], color: 'bg-amber-500' },
  Battery: { hi: 'बैटरी', rate: 180, values: [154, 160, 158, 166, 171, 174, 178, 180], color: 'bg-teal-600' },
};
type Material = keyof typeof series;

export default function MarketBhavPage() {
  const [material, setMaterial] = useState<Material>('Copper');
  const active = series[material];
  const max = Math.max(...active.values);
  const min = Math.min(...active.values);
  return <main className="min-h-screen bg-slate-50 px-5 py-6 lg:px-10"><div className="mx-auto max-w-6xl space-y-6"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-emerald-700">Market Bhav / बाज़ार भाव</p><h1 className="mt-2 text-3xl font-bold text-blue-950">आज का सही रेट जानें</h1><p className="mt-2 text-sm text-slate-600">दिल्ली NCR का 30-day buying rate history</p></div><Badge className="bg-emerald-50 text-emerald-700"><span className="mr-2 h-2 w-2 rounded-full bg-emerald-500" />Live updated today</Badge></header><div className="grid gap-3 sm:grid-cols-4">{(Object.keys(series) as Material[]).map((key) => <button type="button" key={key} onClick={() => setMaterial(key)} className={`border p-4 text-left transition ${material === key ? 'border-blue-600 bg-blue-950 text-white' : 'border-slate-200 bg-white text-blue-950 hover:border-blue-400'}`}><p className="text-sm font-bold">{series[key].hi}</p><p className="text-xs opacity-70">{key}</p><p className="mt-3 text-xl font-bold">₹{series[key].rate}<span className="text-xs font-normal">/kg</span></p></button>)}</div><section className="border border-slate-200 bg-white p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{active.hi} · {material}</p><p className="mt-2 text-4xl font-bold text-blue-950">₹{active.rate}<span className="text-base font-medium text-slate-500">/kg</span></p><p className="mt-2 flex items-center gap-1 text-sm font-semibold text-emerald-700"><TrendingUp className="h-4 w-4" />+{Math.round(((active.rate - active.values[0]) / active.values[0]) * 100)}% in 30 days</p></div><Button variant="outline"><CalendarDays className="mr-2 h-4 w-4" />Last 30 days</Button></div><div className="mt-10 flex h-64 items-end gap-2 border-b border-l border-slate-200 px-3 pb-0 sm:gap-4">{active.values.map((value, index) => <div key={`${value}-${index}`} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><span className="text-[10px] font-bold text-slate-500">₹{value}</span><div className={`w-full max-w-12 ${active.color}`} style={{ height: `${Math.max(16, ((value - min) / (max - min || 1)) * 78 + 18)}%` }} /><span className="pb-2 text-[10px] text-slate-400">{index === 0 ? '1 Aug' : index === 7 ? 'Today' : `${index * 4 + 1} Aug`}</span></div>)}</div><div className="mt-5 flex flex-wrap gap-5 text-sm text-slate-600"><span>30-day low: <strong className="text-blue-950">₹{min}/kg</strong></span><span>30-day high: <strong className="text-blue-950">₹{max}/kg</strong></span><span>Best time to sell: <strong className="text-emerald-700">Today</strong></span></div></section><section className="grid gap-4 md:grid-cols-3"><div className="border border-emerald-200 bg-emerald-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Saathi advice</p><p className="mt-2 font-bold text-blue-950">भाव ऊपर जा रहा है, आज बेचने पर अच्छा दाम मिलेगा।</p></div><div className="border border-slate-200 bg-white p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Rate source</p><p className="mt-2 font-bold text-blue-950">Verified recycler bids</p><p className="mt-1 text-sm text-slate-500">Updated every morning</p></div><div className="border border-blue-200 bg-blue-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-blue-700">Next step</p><p className="mt-2 font-bold text-blue-950">अपने scrap का offer भेजें</p><Button className="mt-3 bg-blue-900 hover:bg-blue-800" onClick={() => window.location.href = '/collector/dashboard'}>Marketplace खोलें <ArrowUpRight className="ml-2 h-4 w-4" /></Button></div></section></div></main>;
}
